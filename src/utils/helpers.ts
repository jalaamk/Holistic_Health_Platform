/**
 * Simple greeting utility function for smoke testing
 */
export function greet(name: string): string {
  return `Hello, ${name}!`;
}

/**
 * Simple addition utility function for smoke testing
 */
export function add(a: number, b: number): number {
  return a + b;
}
